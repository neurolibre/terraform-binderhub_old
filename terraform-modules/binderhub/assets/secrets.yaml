jupyterhub:
  hub:
    services:
      binder:
        apiToken: "${api_token}"
  proxy:
    secretToken: "${secret_token}"
registry:
  url: "${docker_registry}"
  username: "${docker_id}"
  password: "${docker_password}"
