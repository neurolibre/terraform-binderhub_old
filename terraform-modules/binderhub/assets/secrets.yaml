jupyterhub:
  hub:
    services:
      binder:
        apiToken: "${api_token}"
  proxy:
    secretToken: "${secret_token}"
registry:
  url: "binder-registry.conp.cloud"
  username: "${docker_id}"
  password: "${docker_password}"
